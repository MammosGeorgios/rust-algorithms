// TODO: LinkedList in rust  - should study up a bit first though

/*
possible interface to try and recreate:
    get_length() -> usize;
    insertAt(item: i32, index: usize);
    remove(item: i32) -> i32 | None;
    removeAt(index: usize) -> i32 | None;
    append(item: i32);
    prepend(item: i32);
    get(index: usize) -> i32 | None;
 */